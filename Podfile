source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'

# ignore all warnings from all pods
inhibit_all_warnings!

# build dynamic frameworks instead of static
# use_frameworks!

target 'ChatRoom' do
    pod 'NextGrowingTextView'
end
